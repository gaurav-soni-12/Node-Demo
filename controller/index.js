const { getUsers, createUser, deleteUser, updateUser, userLogin, logoutUser } = require("./user-opr");

module.exports = {
    getUsers,
    createUser,
    deleteUser,
    updateUser,
    userLogin,
    logoutUser
};